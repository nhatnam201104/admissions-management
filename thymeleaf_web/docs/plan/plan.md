- Xóa bonus scrore page
- fix logic khi soft deleted, không tạo lại được user có cùng số cccd (bỏ qua bản ghi bị soft deleted)

    - Tính năng thêm sửa tổ hợp môn, các môn được chọn ở dạng select option thay vì nhập tay

- Tạo ngành:
  - Bổ sung thêm phần tạo hệ số cho tổ hợp (bảng xt_nganh_tohop)
  - thay đổi form nhập liệu: tích chọn phương thức nào thì nhập chỉ tiêu cho phương thức đó
  - chỉ tiêu bằng tổng số lượng xét tuyển cộng lại
  - nhập điểm sàn trước, điểm chuẩn sẽ công bố sau
